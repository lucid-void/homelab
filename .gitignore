# MkDocs build output — never commit
site/

# AI
.superpowers/

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# SOPS — encrypted files ARE committed (that's the point); only block decrypted temp outputs
*.sops.tfvars.dec
*.sops.yml.dec
recovery.key

#packer
downloaded_iso_path/

# OpenTofu
*.tfplan
infra/terraform/tfplan
infra/terraform/terraform.tfstate
infra/terraform/terraform.tfstate.backup
infra/terraform/.terraform/
infra/terraform/.terraform.lock.hcl

# Action plan (personal, local only)
PLAN.md

# Kubernetes — plaintext secret templates (seal to *-sealed.yml before committing)
*-secret.yml

# Editor
.DS_Store
.idea/
*.swp
