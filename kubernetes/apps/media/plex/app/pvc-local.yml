---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-config-local
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: openebs-hostpath
  resources:
    requests:
      storage: 50Gi
