---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: shared-tls
  namespace: gateway
spec:
  secretName: shared-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - "*.blackcats.cc"
    - "blackcats.cc"
