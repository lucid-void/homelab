---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: zitadel-bootstrap
  namespace: auth
---
# Allows Terraform kubernetes backend to store state in auth namespace
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: zitadel-bootstrap-state
  namespace: auth
rules:
  # list needed for Terraform backend workspace discovery (resourceNames restriction doesn't apply to list)
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["list"]
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "create", "update", "patch", "delete"]
    resourceNames: ["tfstate-default-zitadel-bootstrap"]
  # leases needed for Terraform state locking (create cannot be restricted by resourceName)
  - apiGroups: ["coordination.k8s.io"]
    resources: ["leases"]
    verbs: ["create"]
  - apiGroups: ["coordination.k8s.io"]
    resources: ["leases"]
    verbs: ["get", "update", "patch", "delete"]
    resourceNames: ["lock-tfstate-default-zitadel-bootstrap"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: zitadel-bootstrap-state
  namespace: auth
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: zitadel-bootstrap-state
subjects:
  - kind: ServiceAccount
    name: zitadel-bootstrap
    namespace: auth
---
# The run-classification state (config hash + last run) lives in a ConfigMap that
# the report container writes itself, so it is deliberately separate from the
# Terraform backend grant above.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: zitadel-bootstrap-report
  namespace: auth
rules:
  - apiGroups: [""]
    resources: ["configmaps"]
    resourceNames: ["zitadel-bootstrap-state"]
    verbs: ["get", "update", "patch"]
  # `create` cannot be restricted by resourceName — the object does not exist yet.
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["create"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: zitadel-bootstrap-report
  namespace: auth
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: zitadel-bootstrap-report
subjects:
  - kind: ServiceAccount
    name: zitadel-bootstrap
    namespace: auth
