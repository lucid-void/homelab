---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: joplin-backup
  namespace: joplin
  annotations:
    # restic and rclone need a writable HOME for their cache
    ignore-check.kube-linter.io/no-read-only-root-fs: "restic/rclone cache"
spec:
  schedule: "0 6 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      activeDeadlineSeconds: 14400
      backoffLimit: 0
      # Self-clean after 24h. failedJobsHistoryLimit alone retains a failed
      # Job until three MORE failures evict it, so one rare failure leaves the
      # object sitting there and KubeJobFailed re-fires ~twice a day forever
      # (minecraft-backup did exactly this for 7 days, 2026-08-20 to 08-28).
      # Nothing is lost: the script's own Gotify ✗ notification carries the
      # log tail and persists in Gotify independently of this object.
      ttlSecondsAfterFinished: 86400
      template:
        spec:
          serviceAccountName: joplin-backup
          restartPolicy: Never
          # joplin-blobs is RWO, and this job has to mount it while the Joplin
          # pod still holds it (the scale-down happens from inside the script).
          # Land on the same node or the mount races. immich's backup skips this
          # only because immich-library is RWX.
          affinity:
            podAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                - labelSelector:
                    matchLabels:
                      app.kubernetes.io/name: joplin
                      app.kubernetes.io/component: app
                  topologyKey: kubernetes.io/hostname
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            fsGroup: 65534
          volumes:
            - name: blobs
              persistentVolumeClaim:
                claimName: joplin-blobs
                readOnly: true
            - name: db-dump
              emptyDir: {}
            - name: rclone-config
              secret:
                secretName: rclone-secret
                defaultMode: 0440
            - name: script
              configMap:
                name: joplin-backup-script
                defaultMode: 0755
          containers:
            - name: backup
              image: ghcr.io/lucid-void/backup-tools:v1.0.2
              command: ["/bin/bash", "/scripts/backup.sh"]
              envFrom:
                - secretRef:
                    name: restic-secret
                - secretRef:
                    name: gotify-secret
                    optional: true
              env:
                - name: PGHOST
                  value: postgres-ro.postgres.svc.cluster.local
                - name: PGPORT
                  value: "5432"
                - name: PGDATABASE
                  value: joplin
                - name: PGUSER
                  value: joplin
                - name: PGPASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: joplin-role-secret
                      key: password
                - name: RCLONE_CONFIG
                  value: /rclone-config/rclone.conf
              volumeMounts:
                - name: blobs
                  mountPath: /blobs
                  readOnly: true
                - name: db-dump
                  mountPath: /db
                - name: rclone-config
                  mountPath: /rclone-config
                  readOnly: true
                - name: script
                  mountPath: /scripts
              resources:
                requests:
                  memory: 256Mi
                  cpu: 100m
                limits:
                  memory: 1Gi
