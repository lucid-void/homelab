---
# Generated with: kubeseal -f kubernetes/apps/paperless/database/app/role-secret.yml -w kubernetes/apps/paperless/database/app/role-sealed.yml
# Replace this file by filling in role-secret.yml with the real password and running kubeseal.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: paperless-role-secret
  namespace: postgres
spec:
  encryptedData:
    password: PLACEHOLDER
    username: PLACEHOLDER
  template:
    metadata:
      name: paperless-role-secret
      namespace: postgres
    type: kubernetes.io/basic-auth
